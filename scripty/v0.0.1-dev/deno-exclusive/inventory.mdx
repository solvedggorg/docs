---
title: "Deno-exclusive inventory"
description: "Catalog of Deno APIs that Bun and Node do not ship as first-class runtime surfaces."
---

# Deno-exclusive inventory

Inclusion rule:

> Native in Deno **and** not first-class in Node **and** not first-class in Bun.

Many Deno I/O methods **overlap conceptually** with Node/`Bun` file APIs. I3 still includes the **`Deno.*` namespace shapes** when the product goal is Deno-shaped source compatibility — but marks rows that are “shape-only wrappers over shared host” as such.

**Pin:** [Sources](/scripty/v0.0.1-dev/reference/sources).

## Permissions (high priority exclusive)

| API | Why exclusive | Notes |
| --- | --- | --- |
| CLI permission flags (`--allow-read`, `--allow-write`, `--allow-net`, `--allow-env`, `--allow-run`, `--allow-ffi`, `--allow-sys`, …) | Deno security model | Map to scripty capability host |
| Permission sets in config | Deno 2.x feature | Align with config story |
| `Deno.permissions.query` | Runtime permission API | — |
| `Deno.permissions.request` | Interactive / programmatic grant | Embed may no-op or policy-hook |
| `Deno.permissions.revoke` | Revocation | — |
| `Deno.PermissionStatus` / descriptors | Types + events | — |

## `Deno` namespace — core runtime

| API | Why exclusive / Deno-shaped | Notes |
| --- | --- | --- |
| `Deno.args` | Deno global shape | — |
| `Deno.mainModule` | Deno entry URL | — |
| `Deno.noColor` | Deno flag | — |
| `Deno.pid` / `Deno.ppid` | Namespace shape | Overlap with `process` — still Deno surface |
| `Deno.hostname` / `Deno.osRelease` / `Deno.loadavg` / `Deno.systemMemoryInfo` / `Deno.uid` / `Deno.gid` | Deno sys APIs | Linux only |
| `Deno.exit` / `Deno.exitCode` | Deno shape | — |
| `Deno.env` (`get`/`set`/`toObject`/`has`/`delete`) | Deno Env object | Not `process.env` |
| `Deno.cwd` / `Deno.chdir` | Deno shape | — |
| `Deno.execPath` | Deno shape | — |

## `Deno` namespace — files & FS

| API | Notes |
| --- | --- |
| `Deno.readFile` / `readTextFile` / `readFileSync` / `readTextFileSync` | Deno shapes over host fs |
| `Deno.writeFile` / `writeTextFile` (+ sync) | — |
| `Deno.open` / `Deno.FsFile` | File handle API |
| `Deno.stat` / `lstat` / `fstat` (+ sync) | — |
| `Deno.mkdir` / `remove` / `rename` / `copyFile` / `link` / `symlink` / `readLink` / `realPath` / `truncate` / `chmod` / `chown` / `makeTemp*` / `create` / `readDir` | Full Deno FS surface |
| `Deno.watchFs` | inotify on Linux |
| `Deno.FileInfo` types | — |

## Net & HTTP

| API | Why exclusive-ish | Notes |
| --- | --- | --- |
| `Deno.listen` / `Deno.listenTls` / `Deno.connect` / `Deno.connectTls` | Deno net API shapes | Bun/Node differ |
| `Deno.serve` | Deno HTTP server | Distinct from `Bun.serve` (I2) and Node `http` |
| `Deno.resolveDns` | Deno DNS API | — |
| `Deno.upgradeWebSocket` | Deno upgrade helper | — |

## Subprocess & workers

| API | Notes |
| --- | --- |
| `Deno.Command` / `Deno.ChildProcess` | Deno subprocess API (not `Bun.spawn` / `child_process`) |
| `Deno.kill` | — |

## Deno KV (exclusive built-in)

| API | Why exclusive | Notes |
| --- | --- | --- |
| `Deno.openKv` | Built-in KV | Not in Node/Bun as runtime builtin |
| Atomic ops / `commit` | — | Local backend first (SQLite/LMDB-style) |
| KV queues / listeners | — | Phase after core KV |
| Remote KV URLs | Optional | May refuse or limit to local for v0 |

## FFI & native

| API | Why exclusive | Notes |
| --- | --- | --- |
| `Deno.dlopen` | Deno FFI | Linux `.so`; permission-gated |
| `Deno.UnsafePointer` / callbacks | FFI detail types | — |

## Testing & tooling APIs

| API | Why exclusive | Notes |
| --- | --- | --- |
| `Deno.test` | Deno test API shape | Distinct from `bun:test` / `node:test` |
| `Deno.bench` | Deno bench API | — |
| Snapshot testing APIs | Deno test helpers | Confirm pin |

## Jupyter / notebook

| API | Why exclusive | Notes |
| --- | --- | --- |
| `Deno.jupyter` | Built-in Jupyter display APIs | Niche; phase late or refuse with reason |

## Other Deno-first surfaces

| API | Notes |
| --- | --- |
| `Deno.cron` (if still runtime API in pin) | Confirm pin |
| OpenTelemetry zero-config hooks unique to Deno | Observability; may be host |
| `Deno.cwd` permission interactions | Tie to permissions |
| Lint plugin runtime APIs | Only if stable runtime surface |

## Intentionally excluded

| Surface | Reason |
| --- | --- |
| `node:` compat under Deno | Shared Node matrix |
| `fetch` / Web streams / WebCrypto | Multi-runtime Web |
| `npm:` specifier mechanics alone | Module host; shared packaging story |
| Deno Deploy-only APIs | Cloud product |
| Full `deno compile` binary product | Tooling CLI, not language runtime API (document separately if needed) |

## Maintenance

1. Diff Deno release notes against inventory each pin bump  
2. Prefer local, permission-gated implementations  
3. When an API becomes shared upstream, reclassify out of I3  
